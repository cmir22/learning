const listTy = [4, 1, 3, 5, 6]
const index = 0

function findNumber(){
    const currentNumber = listTy[index];
    const leftArray = listTy.split(0, currentNumber);
    const rightArray = listTy.split(currentNumber, currentNumber.length);
    let highterLeft = null
    for(i = 0, listTy.length === i, i++){

    }
}